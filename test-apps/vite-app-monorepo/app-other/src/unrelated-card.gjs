const UnrelatedCard = <template>
  <p class="unrelated-card">Unrelated workspace source</p>

  <style scoped inline>
    .unrelated-card {
      color: rgb(30, 20, 10);
    }
  </style>
</template>;

export default UnrelatedCard;
