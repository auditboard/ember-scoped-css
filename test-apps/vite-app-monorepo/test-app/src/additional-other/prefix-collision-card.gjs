const PrefixCollisionCard = <template>
  <p class="prefix-collision-card">Same workspace source outside the root</p>

  <style scoped inline>
    .prefix-collision-card {
      color: rgb(40, 50, 60);
    }
  </style>
</template>;

export default PrefixCollisionCard;
